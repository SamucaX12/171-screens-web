Coloque aqui o scanner compilado com o nome exato:

  171-screens.exe

Como gerar (Visual Studio):
1. Abra `Samuca AC.vcxproj`
2. Config: Release | x64
3. Build → Build Solution
4. O exe sai como `x64\Release\171ScreensAC.exe`
5. Renomeie para `171-screens.exe` e copie para esta pasta

O `ScreensConfig.h` já aponta para `https://171screens.vercel.app`.

Depois faça deploy na Vercel (push no GitHub).
